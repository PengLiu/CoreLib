
logging:
  level:
    root: DEBUG
    org.springframework: DEBUG
    com.coredata: DEBUG
#neo4j的索引需要在正式运行前创建完毕，程序中只做索引的合法性检查
spring:
  redis:
    database: 0  
    host: localhost
    port: 6379
    pool:
      max-active: 8  
      max-wait: -1  
      max-idle: 8  
      min-idle: 0  
